import { writable } from "svelte/store";

export const showMinutePicker = writable(false);
export const showGymSelectionDropdown = writable(false);
export const showSessionTypeSelectionDropdown = writable(false);